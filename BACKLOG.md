# Backlog

Идеи / TODO, не вошедшие в текущую версию. Сортировка — внутри секций по
условному приоритету (сверху вниз).

## В работе

- 🔊 **Звук** — прыжок, монета, удар, победа.
- 💾 **Сохранение прогресса** — какой уровень пройден, лучший счёт.
- 📱 **Touch-управление** — для мобильных.

## Геймплей

- 🎨 **Замена placeholder-графики на настоящие спрайты** — см. `ASSETS.md`,
  весь swap-point локализован в `src/assets.js`.

## Конструктор / редактирование уровней

- 🖱 **Drag-to-paint** — зажать мышь и водить, рисуя линии тайлов сразу.
  Сейчас только клик-по-клику.
- 📁 **Drag-n-drop файл в редактор** — перетащить `.json` на холст →
  загрузился. Сейчас только через «📥 Импорт» с буфера.
- 📤 **Кнопка «Скачать как файл»** — выкладывает `.json` файлом (а не в
  буфер), удобно сразу сохранить.
- 🗂 **Несколько именованных черновиков** (`💾 В черновик` сейчас один слот)
  — низкий приоритет, опубликованные уровни уже мульти-слотовые.

## Тесты / DX

- ⚙️ **pre-commit hook** с `npm test` — не пускать в коммит сломанные
  изменения (непроходимый уровень / `node --check` падает).
- 🎯 **Усиление reachability-теста** — учитывать урон от шипов: «можно
  пройти, но потеряешь все 3 жизни → fail».
- 📦 **`npm run dump-levels`** — официальный скрипт извлечения текущих JSON,
  чтобы не дёргать одноразовый код.

## UX / полировка

- HUD в режиме теста из конструктора показывает «Уровень тест» — некрасиво,
  лучше скрыть поле уровня.
- Стрелки в редакторе иногда уезжают в HTML-инпут (фокус), а не панорамируют
  камеру Phaser.
- Не очевидно, где именно ловится победа вокруг тайла F. Можно подсветить
  hitbox флага в редакторе или сцене.

## Технический долг

- `EditorScene.shutdown` снимает HTML-обработчики, но если Phaser зашатдаунит
  сцену не через стандартный путь — могут утечь. На практике не
  воспроизводилось.

## Изначально вне scope (спека «не-цели»)

- 💾 Прогресс между сессиями → переехало в «В работе» 🎯
- 📱 Touch-управление → переехало в «В работе» 🎯
- 🔊 Звук → переехало в «В работе» 🎯
- 👯 Мультиплеер.
- 🤖 Процедурная генерация уровней.
- ⚙️ Бэкенд (сохранение на сервер, лидерборды, чаты).
