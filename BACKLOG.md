# Backlog

Идеи / TODO. Сортировка — внутри секций по условному приоритету.

## Известные баги

- 🐞 **Мобильная версия не работает** — на реальном телефоне игра неиграбельна
  (тач-управление и/или вёрстка/масштаб холста). Требует диагностики на
  устройстве: проверить срабатывание `pointerType === "touch"`, viewport,
  размер канвы под экран, не перехватывает ли страница жесты (скролл/зум).

## Сделано

- ✅ **Звук** — WebAudio-эффекты (прыжок, монета, удар, убийство врага, победа,
  game over) + mute (`src/sounds.js`).
- ✅ **Сохранение прогресса** — пройденные уровни + лучший счёт в localStorage
  (`src/progressStore.js`), бейджи `✓`/`★` в меню, сброс.
- ✅ **Touch-управление** — удержание половин экрана = ходьба, свайп вверх =
  прыжок.
- ✅ **Графика кодом** — все спрайты рисуются в `src/assets.js` (Kenney убран);
  бесшовные земля/трава, лава (два слоя, верх анимирован), голубое небо, облака.
- ✅ **Кастомный герой по фото** — круглый кроп + анимированный мультяшный
  спрайт (`heroPhoto.js`, `cropOverlay.js`).
- ✅ **Конструктор уровней** — рисование, враги/платформы, тест, экспорт/импорт
  JSON, публикация в меню (`EditorScene.js`).
- ✅ **15 уровней** с плавной кривой сложности + тест проходимости
  (`reachability.test.js`).
- ✅ **Выход в меню в любой момент** — `Esc` и кнопка `≡` в HUD.

## Геймплей

- 🎵 **Фоновая музыка** (сейчас только эффекты).
- ⏱ **Таймер / счёт по времени** прохождения.
- 🪙 **Бонусы/усиления** (доп. жизнь, неуязвимость, двойной прыжок).

## Конструктор / редактирование уровней

- 🖱 **Drag-to-paint** — зажать мышь и водить, рисуя линии тайлов сразу.
- 📁 **Drag-n-drop файл в редактор** — перетащить `.json` на холст.
- 📤 **Кнопка «Скачать как файл»** — `.json` файлом (а не в буфер).

## Тесты / DX

- ⚙️ **pre-commit hook** с `npm test` — не пускать в коммит сломанные изменения.
- 🎯 **Усиление reachability-теста** — учитывать урон от шипов/лавы: «пройти
  можно, но теряешь все жизни → fail».
- 📦 **`npm run dump-levels`** — официальный скрипт извлечения уровней в JSON
  (сейчас одноразовый код).

## UX / полировка

- HUD в режиме теста из конструктора показывает «Уровень тест» — скрыть поле.
- Стрелки в редакторе иногда уезжают в HTML-инпут (фокус), а не панорамируют
  камеру Phaser.
- Не очевидно, где ловится победа вокруг тайла `F` — подсветить хитбокс флага.

## Технический долг

- `EditorScene.shutdown` снимает HTML-обработчики, но при нестандартном
  шатдауне сцены могут утечь (на практике не воспроизводилось).

## Вне scope

- 👯 Мультиплеер.
- 🤖 Процедурная генерация уровней.
- ⚙️ Бэкенд (сохранение на сервер, лидерборды).
